import copyrate from "./copyrate";

export default (directory, resultFileName, filesToUnion) => {};

// Главная функция, движок приложения
// Ничего не возвращает
// Результат работы функции создание итогового файла

// "filesToUnion" представляет собой массив имён файлов для объединения
// "copyrate" используется для каждого файла
